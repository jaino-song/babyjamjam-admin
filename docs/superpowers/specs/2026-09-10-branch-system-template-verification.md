# 메시지 기본 템플릿과 지점별 편집 검증

## 적용 동작

- 새 지점과 수정 이력이 없는 지점은 현재 관리자 기본 템플릿을 사용한다.
- 지점의 첫 실제 수정은 전체 템플릿 내용을 한 번에 보관한다. 그 뒤에는 수정하지 않은 항목을 포함해 관리자 기본값 변경을 받지 않는다.
- 같은 내용을 다시 저장하거나 검증 실패한 요청은 수정 이력을 만들지 않는다. 수정 후 문구를 기본값과 같게 되돌려도 이력은 유지된다.
- 관리자 페이지는 공통 기본값을 수정한다. 메시지 페이지에서는 인증된 지점 구성원이 같은 편집기로 현재 지점의 문구를 수정한다.
- 메시지 페이지의 ‘관리자 페이지에서 수정’ 링크는 제거한다. 문구 편집에는 발신번호 승인이 필요하지 않으며 실제 발송 승인 조건은 유지한다.
- 목록은 서버 카탈로그 전체를 사용한다. 현재 13개이며 클라이언트의 과거 9개 목록으로 제한하지 않는다.
- 저장 요청의 지점은 인증된 세션으로 결정한다. 화면이 기억한 지점과 세션 지점이 다르면 409로 거부한다.
- 자동 발송과 수동 발송 폼은 해당 지점에 적용되는 본문을 읽는다.

## 구현 범위

기존 `SystemTemplateEditor`, 관리자 콘솔, 메시지 페이지, 시스템 템플릿 서비스/훅을 확장했다. 전역 API는 유지하고 지점 조회/수정 API를 추가했다. `branch.system_template_snapshot` JSONB 필드에 전체 문구와 커스텀 변수를 저장하며, 최초 저장과 후속 동시 수정은 트랜잭션과 지점 행 잠금으로 처리한다.

사용자의 dev DB 연결 요청에 따라 로컬 사용자 서버(프론트 4310, API 4311)를 dev DB에 연결했다. 지점 테이블을 백업한 뒤 `20260909172435_add_branch_system_template_snapshot`의 nullable JSONB 열 추가만 적용하고 적용 이력을 기록했다. 관련 없는 마이그레이션이나 운영 배포는 수행하지 않았다. 자동 기능 검사는 별도 로컬 PostgreSQL을 사용했으며 외부 문자 발송은 하지 않았다. 기존 전역 템플릿의 자동화 변수 검증은 보수적인 기존 기준을 유지한다.

## 검증 결과

- 백엔드 보완 검증: 실제 로컬 PostgreSQL을 포함한 6개 테스트 묶음, 323개 통과.
- 통합 프론트엔드: 초기 렌더링·편집·발송 예외·프록시 검사를 포함한 11개 테스트 묶음 75개 통과. 타입 검사 통과. 보완 작업의 별도 7개 묶음 50개 검사와 일부 중복된다.
- 초기 렌더링 회귀 테스트는 기존처럼 쿠키를 바로 읽도록 바꾸었을 때 실패하고, 수정 복원 후 통과했다.
- 실제 로컬 로그인/지점 선택 후 권한 확인: 비로그인 401, 일반 사용자의 전역 수정 403, 다른 지점 선택 403, 이전 지점 저장 요청 409.
- 실제 관리자 화면에서 기본값 저장 → 일반 A지점 사용자 메시지 화면에서 첫 수정 → 관리자 화면에서 다른 항목 변경 → A지점 전체 유지 / B지점 최신값 반영을 확인했다. 새로고침 후에도 지점 문구가 유지됐다.
- 지점 목록 13개 표시, 관리자 수정 링크 미노출, 발신번호 승인 전 편집 가능을 확인했다. 실제 발송은 수행하지 않았다.
- 로컬 Playwright 기능 검사에서 관리자/메시지 화면의 페이지 오류는 0개였다. 1280px 화면의 본문 편집 너비는 각각 약 445px/471px, 375px에서는 약 358px이었다. 스크린샷을 직접 확인해 관리자 편집 칸이 좁아지는 배치를 수정했다.
- UI 부채 기준선은 메시지 페이지의 기존 35개 항목 위치만 갱신했다. 종류·개수의 증가를 허용하지 않았다.
- 의존성 감사는 기존 취약점 14개(low 1, moderate 5, high 6, critical 2)를 보고했다. 이번 작업은 의존성 파일을 변경하지 않았다.

기능 검사 결과는 `/tmp/bjj-template-journey-results.json`, `/tmp/bjj-template-layout-results.json`에, 테스트 출력은 `/tmp/bjj-template-backend-tests.log`, `/tmp/bjj-template-frontend-tests.log`, `/tmp/bjj-template-hydration-tests.log`에 남겼다. 로컬 경로이므로 CI나 운영 검증을 대신하지 않는다.

## 독립 검토 후 보완

첫 독립 검토에서 지점 전환 후 남은 발송 초안, 템플릿 조회 실패 시 수동 폼의 하드코딩 문구, 자동 발송의 알 수 없는 조회 오류 처리 등 세 가지 결함을 확인했다.

- 지점 전환 시 수신자·변수·발송 대기 목록·미리보기·선택 상태를 초기화하고, 같은 지점의 작성 내용은 유지한다. 비동기 중복 검사 중 지점이 바뀌어도 이전 요청은 발송하지 않는다.
- 수동 SMS 요청에 화면이 기억한 지점 식별자를 전달한다. 서버는 인증된 지점과 비교해 불일치 요청을 수신자 조회·로그 생성·외부 발송 전에 거부한다.
- 8개 기존 수동 폼은 해당 지점의 템플릿을 읽기 전이나 조회 실패 시 발송을 막는다. 이미 읽은 본문은 표시할 수 있지만 하드코딩 문구로 대체해 발송하지 않는다.
- 자동 발송은 분류되지 않은 템플릿 조회 오류를 기본 문구로 바꾸지 않고 실패로 전달한다. 기존 일시 오류 재시도 처리는 유지한다.

수정 전 실제 로컬 화면에서 이전 지점 초안의 발송 요청 1건과 템플릿 조회 실패 시 활성화된 발송 버튼을 재현했다. 수정 후 같은 검사에서 이전 지점 발송 요청은 0건, 새 지점의 수신자는 빈 값, 이전 초안은 미노출이었다. 조회 지연·실패 상태에서는 발송 버튼이 비활성화됐다. 실제 서버 HTTP 요청도 지점 불일치 시 `409 BRANCH_CONTEXT_CHANGED`를 반환했다. 브라우저 발송 요청은 가로채어 검사했으며 외부 문자는 보내지 않았다. 검사 중 변경한 로컬 지점의 발신번호 승인 상태는 원래 값으로 복원했다.

보완 증거: `/tmp/bjj-template-backend-correction-all.log`, `/tmp/bjj-template-frontend-correction-all.log`, `/tmp/bjj-manual-switch-green.log`, `/tmp/bjj-manual-template-failure-green.log`, `/tmp/bjj-manual-http-context.log`. 통합 화면의 1280px/375px 레이아웃 및 오류 검사도 다시 통과했다. 기존 UI 부채 35개는 줄 위치만 다시 반영했으며 파일별·규칙별 개수는 동일하다.

## 중단된 Claude 편집기 작업 재개

Claude의 기존 개인 템플릿 편집기 회귀 테스트와 `TemplateContentEditor` 추출 작업을 이어받았다. 원래 중단된 `unit-1.1`의 커밋과 미추적 테스트 파일은 보존했으며, 별도 작업 단위에서 구현한 결과만 현재 작업 브랜치에 통합했다.

- 관리자와 지점 기본 템플릿은 같은 공통 칩 편집기를 사용한다. 빠른 변수 삽입, 변수 칩 클릭 시 읽기 전용 팝오버, 바이트/SMS/LMS 표시를 제공한다. 개인 템플릿의 변수 정의 편집은 유지한다.
- 시스템 템플릿에는 서버 레지스트리와 기존 커스텀 변수만 표시한다. 개인 템플릿용 프리셋 10개나 새 변수 정의 UI를 추가하지 않는다. 기존 커스텀 변수는 저장 요청에 그대로 포함한다.
- 편집 화면의 별도 발송 예시·정보 패널을 제거했다. 미리보기는 별도 탭에 두며 탭 이동 후에도 작성 중인 초안을 유지한다. 관리자 버전 복원은 반환된 문구로 초안을 즉시 초기화한다.
- 변수 정보가 없거나 null일 때 반복 렌더링하지 않도록 입력 배열을 안정화했다. 수정 전 저장 버튼은 비활성이고, 변수 팝오버를 여는 것만으로 수정 상태가 되지 않는다.
- 모바일에서 하단 메뉴가 새 저장 버튼을 가리는 실패를 실제로 재현한 뒤, 두 편집 패널의 모바일 하단 여백만 보완했다. 375px에서 관리자와 지점 저장 버튼 모두 가림 없이 클릭 가능한 것을 확인했다. 이 검사는 문자를 보내지 않는 클릭 가능성 검사다.

통합 프론트엔드 검증은 13개 묶음 86개 테스트와 타입 검사가 통과했다. 개인 템플릿의 기존 동작, 공통 편집기, 시스템 편집기, 관리자·메시지 화면, 지점 전환, 조회 실패 시 발송 차단, 프록시를 포함한다. 백엔드 변경은 없어 이전 323개 테스트 및 독립 검토 증거를 재사용한다.

로컬 기능 검사는 별도 프론트 4320/API 4321 및 PostgreSQL 55439의 테스트 DB에서 수행했다. 관리자 저장 → 일반 A지점 사용자 첫 수정 → 관리자 다른 항목 변경 → A 전체 유지/B 최신 기본값 반영이 통과했다. 모든 13개 항목 표시와 새로고침 후 문구 유지, 이전 지점 초안의 저장 요청 0건도 다시 확인했다. 변수 포함 템플릿은 화면의 토큰·줄바꿈과 실제 API 저장 본문이 정확히 같고 커스텀 변수가 유지되는 것을 확인했다. 페이지 오류는 0개였다.

1280px/375px 스크린샷을 직접 확인했다. 편집기 본문 너비는 관리자 약 445px/358px, 지점 약 471px/358px이다. 전체 모바일 탐색 검수를 뜻하지는 않는다. 375px에서 처음 진입할 때 기존 고정 헤더가 메시지의 템플릿 메뉴를 가리는 현상이 별도로 관찰되어, 편집기 검사는 상세를 연 뒤 화면을 375px로 줄여 진행했다. 이번 수정은 공통 헤더나 전체 모바일 탐색 구조를 변경하지 않는다.

증거: `/tmp/bjj-editor-integrated-tests.log`, `/tmp/bjj-editor-integrated-typecheck.log`, `/tmp/bjj-editor-journey-results.json`, `/tmp/bjj-editor-switch-check.log`, `/tmp/bjj-editor-chip-roundtrip.log`, `/tmp/bjj-editor-layout-results.json`, `/tmp/bjj-editor-mobile-actions.log`(실패), `/tmp/bjj-editor-mobile-actions-green.log`(보완 후 통과). 이 결과는 공식 실제 Chrome 최종 검수나 CI 통과를 의미하지 않는다.

## 공통 편집기 독립 검토 후 보완

`076cf4136`에 대한 독립 검토는 저장 중 입력 잠금과 실제 편집 영역의 라벨 연결 두 항목을 보완 대상으로 판정했다.

- 저장 중 상태를 공통 편집기까지 전달해 Tiptap 자체를 비편집 상태로 전환한다. 변수 삽입도 비활성화하고, 직접 삽입 명령과 붙여넣기 경로에서 비편집 상태를 확인한다. 빠른 삽입은 기존 Badge 모양을 유지한 실제 버튼으로 구성했다.
- 숨겨진 textarea 미러를 제거했다. 실제 contenteditable에 고유 id, textbox 역할, 여러 줄 입력 속성, 라벨 관계를 연결하고 라벨 클릭 시 이 편집기로 포커스를 보낸다.
- 시스템 편집기와 메시지 페이지 테스트는 실제 이름 붙은 contenteditable에 전체 선택·붙여넣기를 수행한다. 이전 미러 입력 칸을 조작하는 경로는 제거했다. 저장 중 붙여넣기/삽입 차단 및 저장 후 편집 재개, 라벨 포커스 회귀 검사를 추가했다. 개인 템플릿의 고정된 기존 5개 테스트는 수정 없이 통과한다.

실제 브라우저에서 저장 응답을 보류한 상태로 재현한 수정 전 결과는 `editable: true`, 화면과 저장 초안 불일치, 숨겨진 미러 1개, 이름 붙은 실제 편집기 0개였다. 보완 후 관리자와 지점 화면 모두 `editable: false`, 불일치 없음, 미러 0개, 이름 붙은 편집기 1개를 확인했다. 라벨 클릭 포커스와 변수 버튼 비활성화도 통과했다. 이 보류 검사는 저장 응답을 가로채므로 외부 문자를 발송하지 않는다. 별도로 실제 테스트 API 저장을 통해 토큰·줄바꿈·커스텀 변수 보존 및 저장 후 정상 편집 상태 복귀를 다시 확인했다.

최종 보완 검증: 13개 묶음 88개 테스트, 타입 검사, 변경 파일 ESLint(오류·경고 0), UI 규칙 검사, diff 검사가 통과했다. 기존 React/Tiptap 테스트의 `act` 경고는 남아 있다. 증거는 `/tmp/bjj-editor-correction-all-tests.log`, `/tmp/bjj-editor-correction-typecheck.log`, `/tmp/bjj-editor-correction-lint.log`, `/tmp/bjj-editor-correction-ui-gate.log`, `/tmp/bjj-editor-pending-red.log`, `/tmp/bjj-editor-pending-green.log`, `/tmp/bjj-editor-branch-pending-green.log`, `/tmp/bjj-editor-final-chip-roundtrip.log`에 있다.

## 병합 정리 세션 — suggestion-lock 통합과 dev 재동기화 (2026-09-10)

중단됐던 suggestion-lock 작업 단위의 미커밋 변경을 검증·커밋해 이 브랜치에 통합했다(`392e16077`). 저장 중 편집 잠금 상태에서 두 변수 자동완성 플러그인을 종료하고, 비편집 상태의 suggestion 삽입 명령을 거부하며, 플러그인 키를 모듈 수준으로 안정화했다. 신규 단위 테스트와 저장 잠금 팝업 소멸 통합 테스트가 이 동작을 고정한다. 독립 감사는 `exitSuggestion` → `onExit` 언마운트 경로와 렌더·삽입 전면 가드를 라이브러리 구현까지 대조해 확인했고 blocker 없이 통과했다(제안 3건은 비차단: 테스트의 Enter 키 핀 중복성, 재활성화 후 dismissedRange 동작, 기존 `act` 경고).

이어서 `origin/dev` 14커밋을 이 브랜치로 머지했다(`21cf8c528`). 충돌 해소 원칙은 양쪽 의지의 보존이었다.

- 발송 폼 레이아웃은 브랜치의 `templateReady`(지점 템플릿 로딩·실패 시 발송 차단)와 dev의 `receiptLinkPreparation`(영수증 링크 준비물)을 모두 유지한다.
- `ServiceRecordLinkMessageForm`은 dev의 영수증 모드(템플릿 키 분기 `SERVICE_END_NOTICE`/`SERVICE_RECORD_LINK`)를 취하되, 양쪽 모드 모두 하드코딩 폴백 문구를 제거하고 `systemTemplate?.content`만 렌더링한다. 조회 실패 시 본문은 빈 값이고 `templateReady`가 발송을 막는다(브랜치의 fail-closed 계약).
- `TemplateSendForm`은 브랜치의 지점 컨텍스트·템플릿 게이트(`!branchContextReady || !templateReady`)와 dev의 준비링크 일반화(`isPreparedLinkDelivery`, 영수증 준비물 게이트)를 하나의 검증·제출 체인으로 합친다. 지점 컨텍스트 미정렬 시 수신자 입력은 상태 배지로 대체된다.
- 메시지 페이지의 `SERVICE_END_NOTICE`는 병합된 동작대로 영수증 링크 발송 폼으로 라우팅되며, 테스트도 이를 따른다. dev가 되살린 `messages/system-templates/[templateKey]` UI 부채 항목은 이 브랜치가 해당 페이지를 리다이렉트로 교체했으므로 다시 제외했다.

자동 머지가 놓친 시맨틱 갭 두 건을 전체 게이트에서 잡아 수정했다(`ab1000e75`). 첫째, `sms-trigger-payload-enricher.spec.ts`는 구 `getByKey` mock과 소실된 registry 폴백 의미론을 유지하고 있었는데, 서비스의 `getByKeyForBranch` fail-closed 계약에 맞춰 기본 지점 템플릿을 resolve하도록 고쳤다. 둘째, `TriggerRulesManager` 테스트는 브랜치 스코프가 기본값이 된 `useSystemTemplate`이 요구하는 선택 지점 쿠키가 없어 데이터가 강제로 undefined였는데, 지점 쿠키를 심어 정렬했다. UI 부채 기준선은 게이트가 보고한 35 제거/35 추가(전부 위치 이동, 종류·개수 불변)를 확인한 뒤 병합 위치로 재고정했다(`a1d06a851`).

최종 게이트: 백엔드 350개 묶음 4921개 테스트 0 실패(1 묶음 skip), 프론트 227개 묶음 1435개 테스트 0 실패, 양쪽 타입 검사 통과, UI 규칙 검사 통과, `env-check` 경고만 있고 exit 0. 증거는 `/tmp/bjj-320-final-backend2.log`, `/tmp/bjj-320-final-backend2-typecheck.log`, `/tmp/bjj-320-final-frontend2.log`, `/tmp/bjj-320-final-frontend2-typecheck.log`, `/tmp/bjj-320-final-envcheck.log`, `/tmp/bjj-320-ui-gate-green.log`에 있다. 병합 해소에 대한 독립 감사도 blocker·must-fix 없이 통과했으며, 비차단 제안으로 `sendReceiptLink`의 방어적 게이트 보강, 모바일 `useSystemTemplate`의 전역 조회 이행(기존 과제, PR #57 이후), 테스트 헬퍼의 `updatedAt` 포함 세 가지가 기록됐다.

## 남은 승인 및 제한

`dev` 병합과 운영 배포는 별도 단계다. 최종 실제 Chrome 화면 검수에 요구된 `chrome:control-chrome` 도구/스킬은 이 세션에서도 사용할 수 없었다(검증 세션과 동일 제약). 위 Playwright 검사와 자동 게이트는 기능·레이아웃 보조 증거이며, 해당 스킬이 요구하는 최종 실제 Chrome 검수 통과로 간주하지 않는다. 최종 검수는 `chrome-ui-verification` 스킬을 보유한 별도 세션에서 수행해야 하며, 독립 검토 상태는 작업 완료 보고에서 구분한다.
