# 용어 사전 (Terminology Dictionary)

Generated by AI Conversation Designer - 2026-02-10

---

## System Prompt Section

Add this section to `backend/application/services/ai-chat.service.ts` in the SYSTEM_PROMPT constant.

```markdown
## 용어 사전 (Terminology Dictionary)

다음 용어들은 같은 의미입니다. 사용자가 어떤 용어를 사용하든 동일하게 이해하세요.

### 산모 (Client)
서비스를 받는 이용자입니다.
- **표준 용어**: 산모
- **동의어**: 이용자, 고객, 임산부, 바우처 이용고객, 일반 고객, 자부담 고객, 임신 중인 사람, 신부
- **제외된 용어**: 엄마 (일반적 의미와 혼동 가능)

### 제공인력 (Employee)
서비스를 제공하는 관리사/돌보미입니다.
- **표준 용어**: 제공인력
- **동의어**: 관리사, 이모님, 직원, 돌보미, 이모, 근무자, caregiver, staff, worker, 시어머니, 친정 엄마, 산모신생아건강관리도우미

### 계약서 (Contract) - ⚠️ 중의적 용어
"계약서" 또는 "document"라고만 말하면 어떤 계약서인지 물어보세요:
- **서비스 계약서**: 산후도우미 이용 계약 (현재 지원)
- **직원 고용 계약서**: 향후 추가 예정

**중의적 표현 발견 시 응답 예시:**
> "어떠한 계약서를 말씀하시는 건가요?
> - 서비스 계약서 (산후도우미 이용 계약)
> - 직원 고용 계약서 (향후 지원 예정)"

### 일정 (Schedule)
- **표준 용어**: 일정
- **동의어**: 스케줄, 배정 현황, schedule

---

**중요**: 용어가 다르더라도 같은 엔티티를 지칭하면 동일하게 처리합니다.

예시:
- "이용자 몇 명?" = "산모 몇 명?" = "고객 수는?" → getDashboardStats
- "관리사 검색" = "이모님 찾아줘" = "돌보미 찾기" → searchEmployees
```

---

## TypeScript Implementation

```typescript
// terminology-map.ts

export const TERMINOLOGY_MAP = {
  Client: {
    standard: '산모',
    synonyms: [
      '이용자', '고객', '임산부', '바우처 이용고객',
      '일반 고객', '자부담 고객', '임신 중인 사람', '신부'
    ],
    excluded: ['엄마'], // Intentionally excluded due to ambiguity
    patterns: [
      /산모/i, /이용자/i, /고객/i, /임산부/i,
      /바우처\s*이용고객/i, /일반\s*고객/i, /자부담\s*고객/i,
      /임신\s*중인\s*사람/i, /신부/i, /client/i
    ],
    description: '서비스를 받는 이용자 (Service recipients)'
  },

  Employee: {
    standard: '제공인력',
    synonyms: [
      '관리사', '이모님', '직원', '돌보미', '이모', '근무자',
      'caregiver', 'staff', 'worker',
      '시어머니', '친정 엄마', '산모신생아건강관리도우미'
    ],
    patterns: [
      /제공인력/i, /관리사/i, /이모님/i, /직원/i, /돌보미/i,
      /이모(?!님)/i, /근무자/i, /caregiver/i, /staff/i, /worker/i,
      /시어머니/i, /친정\s*엄마/i, /산모신생아건강관리도우미/i,
      /employee/i
    ],
    description: '서비스를 제공하는 관리사 (Service providers)'
  },

  Contract: {
    standard: '서비스 계약서',
    synonyms: [
      '산후도우미 계약서', '서비스 이용 계약서'
    ],
    ambiguous: ['계약서', 'document', '서류', '문서'], // Requires disambiguation
    patterns: [
      /서비스\s*계약서/i, /산후도우미\s*계약서/i,
      /서비스\s*이용\s*계약서/i
    ],
    disambiguationPatterns: [
      /계약서/i, /document/i, /서류/i, /문서/i
    ],
    description: '서비스 이용 관련 계약서'
  },

  Schedule: {
    standard: '일정',
    synonyms: ['스케줄', '배정 현황', 'schedule'],
    patterns: [
      /일정/i, /스케줄/i, /배정\s*현황/i, /schedule/i
    ],
    description: '관리사 배정 일정'
  }
} as const;

// Detection function
export function detectEntity(input: string): keyof typeof TERMINOLOGY_MAP | null {
  const normalized = input.toLowerCase();

  for (const [entity, config] of Object.entries(TERMINOLOGY_MAP)) {
    if (config.patterns.some(p => p.test(normalized))) {
      return entity as keyof typeof TERMINOLOGY_MAP;
    }
  }

  return null;
}

// Disambiguation check
export function requiresDisambiguation(input: string): string[] | null {
  const contract = TERMINOLOGY_MAP.Contract;
  if (contract.disambiguationPatterns?.some(p => p.test(input))) {
    // Check if specific contract type is mentioned
    if (!contract.patterns.some(p => p.test(input))) {
      return ['서비스 계약서 (산후도우미 이용 계약)', '직원 고용 계약서 (향후 지원 예정)'];
    }
  }
  return null;
}
```

---

## Validation Checklist

- [ ] All Client synonyms trigger client-related tools
- [ ] All Employee synonyms trigger employee-related tools
- [ ] "계약서" alone triggers disambiguation question
- [ ] "서비스 계약서" directly triggers contract tools
- [ ] No cross-entity confusion (e.g., "이모" doesn't match Client)
